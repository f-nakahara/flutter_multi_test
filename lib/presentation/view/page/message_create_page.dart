import 'package:flutter/material.dart';

class MessageCreatePage extends StatelessWidget {
  const MessageCreatePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('メッセージ作成画面'),
      ),
    );
  }
}
